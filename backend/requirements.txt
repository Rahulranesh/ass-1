# Backend requirements for Render deployment
# Install with: pip install -r requirements.txt

# Web framework
flask==3.0.3
flask-cors==4.0.1

# WSGI server for production (Render uses this)
gunicorn==22.0.0

# PostgreSQL adapter
psycopg2-binary==2.9.9

# JWT (PyJWT — not jwt, which is a different package)
PyJWT==2.8.0

# Password hashing
bcrypt==4.1.3

# AWS SDK (kept for compatibility if switching to AWS later)
boto3==1.34.115
botocore==1.34.115

# Testing
pytest==8.2.2
pytest-cov==5.0.0
