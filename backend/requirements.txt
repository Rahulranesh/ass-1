# Backend requirements — FastAPI on Render
# Install with: pip install -r requirements.txt

# Web framework
fastapi==0.115.5

# ASGI server (production)
uvicorn[standard]==0.32.1

# PostgreSQL adapter
psycopg2-binary==2.9.9

# JWT
PyJWT==2.8.0

# Password hashing
bcrypt==4.1.3

# Data validation (used by FastAPI internally + our Pydantic models)
pydantic[email]==2.9.2

# AWS SDK (kept for compatibility if switching to AWS later)
boto3==1.34.115
botocore==1.34.115

# Testing
pytest==8.2.2
pytest-cov==5.0.0
httpx==0.27.2          # for FastAPI TestClient
